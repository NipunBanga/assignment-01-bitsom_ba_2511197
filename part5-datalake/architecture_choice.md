## Architecture Recommendation

A Data Lakehouse is recommended because it supports structured, semi‑structured, and unstructured data. ICU streams, patient records, clinical notes can all be stored in a scalable storage layer while still allowing analytical queries similar to a data warehouse. This architecture enables both analytics and machine learning workloads efficiently.

