## Architecture Recommendation

A Data Lakehouse is recommended because it supports structured, semi‑structured, and unstructured data. GPS logs, reviews, transactions, and images can all be stored in a scalable storage layer while still allowing analytical queries similar to a data warehouse. This architecture enables both analytics and machine learning workloads efficiently.